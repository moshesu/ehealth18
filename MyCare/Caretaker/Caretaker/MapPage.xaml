<Page
    x:Class="Caretaker.MapPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Caretaker"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:maps="using:Windows.UI.Xaml.Controls.Maps"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
          
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Button Grid.Column="1" Grid.Row="0" Style="{StaticResource RoundButton}" Height="30" Width="65" Margin="0,0,0,0" HorizontalAlignment="Right" VerticalAlignment="Center" Click="Info_Click" FontWeight="Bold" Background="#FFA7A9AC">Back </Button>
        <TextBlock Grid.Row="0" Grid.Column="1" TextWrapping="Wrap" x:Name ="errormessage" FontWeight="Bold" Foreground="#FF8B0B04" VerticalAlignment="Top"/>
        

        <maps:MapControl x:Name="PMap"
                         Grid.Row="1"
                         Loaded="Map_Loaded"></maps:MapControl>
    </Grid>
</Page>
